en:
  date:
    formats:
      default: '%-d %B %Y'
      with_weekday: '%a %m/%d/%y'

  time:
    formats:
      default: '%a, %b %-d, %Y at %r'
      date: '%b %-d, %Y'
      short: '%B %d'

  layouts:
    app_name: thoughtbot
